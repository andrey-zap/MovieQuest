import React from "react";
import ReactDOM from "react-dom/client";
import MovieQuiz from "./MovieQuiz.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MovieQuiz />
  </React.StrictMode>
);
